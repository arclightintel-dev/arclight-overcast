# ECR module variables
#
# Expected inputs:
#   - repositories (list of repository names)
#   - image_tag_mutability (default IMMUTABLE)
#   - max_image_count (lifecycle policy)
#   - github_oidc_role_arn (for push access)
