# Secrets module variables
#
# Expected inputs:
#   - environment
#   - service_secrets (map of service → list of secret names)
#   - service_parameters (map of service → list of SSM parameter names)
