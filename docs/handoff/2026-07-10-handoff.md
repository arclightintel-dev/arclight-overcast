# Session Handoff — 2026-07-10

> **Purpose**: Drive the NEXT session cold. Assume no memory of today. Everything needed to be
> productive in 5 minutes is here.
>
> **Written**: 2026-07-11 | **Covers session**: 2026-07-10
> **HEAD at handoff**: `5f74a55` on `main` (PR #1 merged) | **AWS**: 650880817826 / us-east-1

---

## Read-first orientation

1. This doc, then `docs/PROJECT_STATE.md`, `docs/CONSTITUTION.md`, `docs/DECISIONS.md` (esp. **O-009**).
2. **`docs/PROJECT_STATE.md` is STALE** — it is pinned at HEAD `d1c9b7f` but `main` is now 6 commits
   ahead at `5f74a55`. It lists controller `v2-phase2-fix2` / workspace `v1-fix1`; the true current
   values are **`v2-phase2-fix3` / `v1-fix2`** (verified in `terraform/envs/staging/variables.tf`).
   Refresh PROJECT_STATE early next session.
3. **The crux of next session is the deployment/CI model** — see §3 (blocker) and §4 (scoping brief).
   Do the scoping pass BEFORE more ad-hoc CI fixes. This is explicit user direction.

**Verification note**: §1, §3, §6, §7 are verified against code at `5f74a55`. §2 "live state" mixes
code-verified image tags with AWS-side runtime facts (table counts, task-def revisions, running IPs)
that were **not** re-queried from AWS in this doc-writing pass — treat those as last-known and
confirm via AWS/console at session start.

---

## 1. Session summary (2026-07-10)

Big session. Phase 5 substrate went from partial to controller-LIVE, plus a full CI deploy pipeline.

| Delivered | Detail | Anchor |
|-----------|--------|--------|
| **coturn TURN server — LIVE** | Full spec→plan→review→implement→deploy. EIP **52.72.36.174**, G4/G5 smoke passed. Reversed O-002 rollback with a proper spec first (INV-005). | `a5c04ad` |
| **Dedicated workspace subnets** | `10.0.30.0/24` + `10.0.31.0/24` added to VPC. coturn allowed-peer scoped to these; denied-peer for all internal. | (in coturn wiring) |
| **Core Phase 6A deploy** | Advanced to **`v2-8c2f8c8`** (asset backend). Deploy was via the pipeline (out-of-band), not a TF commit. | pipeline |
| **Podbay controller — LIVE on Fargate** | `podbay.staging.arclight-complex.net`, image **`v2-phase2-fix3`**, all ready checks green (database, jwks_cache, substrate_adapter ECSAdapter, connection_registry, seed_data, recipe_available). O-003 = Fargate not EC2. | `d5169fd`, `e0dcac5`, `fdda3f6` |
| **Cloudflare Access gate** | Live on `*.staging.arclight-complex.net` (one-time-PIN IdP + automation service token for health checks). | `8a6b777` |
| **Self-serve deploy pipeline (O-009)** | `deploy-service.yml` now supports `repository_dispatch` (cross-repo, module-triggered) alongside `workflow_dispatch`. Verifies ECR image → registers task def → updates service → waits stable → health-checks through CF Access. | `8a6b777`, `5ebb25c`, `770b510` |
| **Deploy role IAM** | `ecr:DescribeImages` + `PassRole` (exec **and** task roles) + env-scoped OIDC + `TagResource` scope fix. | `64a25b4`, `9b054d7`, `4f292d1`, `0b627fb` |
| **`ignore_changes=[task_definition]`** | `ecs-service-fargate` module so `terraform apply` no longer reverts pipeline-deployed images. Verified `terraform/modules/ecs-service-fargate/main.tf:126-128`. | `d1c9b7f` |
| **Workspace image → v1-fix2** | Podbay-side supervisord/Neko entrypoint fix so the X server starts. | `da8de9d` |
| **ECS task-def JSON churn fix** | Normalized workspace task-def template + pinned `podbay_image_tag` (dropped `"latest"` default). Merged via **PR #1**. | `38a3f46`, `d674349`, `5f74a55` |

**INV-005 / spec discipline win**: coturn had failed 4 apply-fix cycles previously (O-002). This time
it went spec→plan→review→implement→deploy and landed clean. Keep that sequence.

---

## 2. Current live state — staging

Source of truth for image tags = `variables.tf` defaults (O-009: "pinned to the live deploy base").
Because of `ignore_changes=[task_definition]`, **AWS is authoritative for what's actually running** —
verify with `aws ecs describe-services` at session start.

| Component | Image / value | Status | Notes |
|-----------|---------------|--------|-------|
| **Core** | `v2-8c2f8c8` | LIVE | Phase 6A; ~19 tables, asset_backend ok (last-known, verify) |
| **Podbay controller** | `v2-phase2-fix3` | LIVE, all checks green | Fargate, `podbay.staging.arclight-complex.net` |
| **Podbay workspace task def** | `v1-fix2` | Registered (last-known **rev 14**) | EC2 capacity provider; startup blocked (§3) |
| **coturn** | EIP `52.72.36.174` | LIVE | Dedicated workspace subnets `10.0.30/31.0/24` |
| **TURN secret** | shell | Populated out-of-band | Secrets Manager shell only (per repo rule) |
| **Asset encryption key** | shell | Populated out-of-band | |
| **Cloudflare Access gate** | `*.staging.arclight-complex.net` | LIVE | One-time-PIN + automation service token |
| **ALB** | Cloudflare CIDRs only | LIVE | O-005 hardening |
| **EIP quota** | 10 | Raised | Was default 5 |

**Prod**: substrate only, no services. RDS multi-AZ, ALB Cloudflare-restricted, DBs bootstrapped,
secret shells created. No pipeline (operator-applied per D-059 §6).

---

## 3. Blockers

### 3.1 Podbay workspace container startup — Podbay-side, infra complete

Workspace container exits **code 2** on launch. This is a Podbay container issue, **not infra** — all
Overcast substrate is done (subnets, SG rules, exec role, controller task role, S3 export bucket,
managed scaling, coturn). Podbay shipped **`v1-fix2`** (supervisord/Neko entrypoint fix) and is
retrying. Blocks the E2E integration smoke. **Owner: Podbay.** No Overcast action unless the retry
surfaces a new infra gap.

### 3.2 `terraform-apply.yml` (CI auto-apply) is BROKEN — the crux of next session

**Every apply this session was MANUAL** (local, `john-admin` admin creds). CI auto-apply has never
succeeded post-hardening. Two independent causes — **both** must be fixed, and fixing one alone still
leaves CI red:

**Cause (a) — Terraform version incompatibility in a validation block.**
`terraform/modules/iam-github-oidc/variables.tf:53-56` validates `oidc_provider_arn` with a condition
that references *another* variable:
```hcl
validation {
  condition     = var.create_oidc_provider || var.oidc_provider_arn != null
  error_message = "oidc_provider_arn is required when create_oidc_provider is false."
}
```
Cross-object references inside `validation` conditions require **Terraform ≥ 1.9**. CI pins
`terraform_version: '~> 1.5'` (resolves to 1.5.7) in **both** `terraform-apply.yml:33` **and**
`terraform-plan.yml:32`, so CI fails with **"Invalid reference in variable validation"**. Local
Terraform is **v1.15.6**, which accepts it — so it passes locally and fails only in CI. This breaks
the PR-plan workflow too, not just apply.

**Cause (b) — required variables have no default and `terraform.tfvars` is gitignored.**
Four variables have **no default**: `domain_name`, `alarm_email`, `github_org`, `core_image_tag`
(`variables.tf:7,66,71,82`). Locally they come from `terraform/envs/staging/terraform.tfvars`, which
is **gitignored** (confirmed via `git check-ignore`). CI has no `terraform.tfvars`, so even with cause
(a) fixed, `terraform apply` fails on missing required variables.

> Do **not** hot-patch these blindly next session. The user wants a definitive model first (§4).

---

## 4. NEXT SESSION — PRIMARY: deployment-model scoping brief

**Explicit user direction**: START next session with a scoping / research pass — understand
GitHub + AWS + Terraform deeply and define a **definitive** model for deployment, staging, and CI —
**before** more ad-hoc fixes. Treat this as a spec step (spec→plan→review→implement), not a patch.

**Scope guard** — this is a *decision brief, not an open-ended research pass*. Most load-bearing facts
were learned the hard way this session (§3, §7); the work is choosing among known options, not
discovering how the platforms work. Size the model to **3 modules + one staging environment**
(governance is earned) — not a hypothetical multi-env / multi-team future. No blue/green, no GitOps
controllers, no prod model beyond D-059 §6 (operator-applied). Process: claude-main drafts the brief →
hotpants gut-checks domain realism → ratify as an O-decision (cross-repo-auth piece escalates to
D-series). Lighter than the full 5-artifact coturn treatment. **No urgency pressure** — manual apply
works, deploys flow through the pipeline, nothing is blocked; that's the condition to do it right.

Open questions this scoping must answer:

| # | Question | Tension / context |
|---|----------|-------------------|
| Q1 | How are TF variables (image tags + `domain_name`/`alarm_email`/`github_org`) supplied in CI when `terraform.tfvars` is gitignored? | Options: commit a non-secret `staging.auto.tfvars` (none are truly secret?) · variable defaults · CI-injected `-var`/`TF_VAR_*` · SSM/Param Store. Image tags are already handled out-of-band by the pipeline (`ignore_changes`) — does CI even need `core_image_tag`? |
| Q2 | What Terraform version does CI pin, and how do local + CI stay in sync? | The `~> 1.5` vs local `1.15.6` gap caused cause 3.2(a). Pin an exact version both places (`.terraform-version` / `required_version`)? |
| Q3 | Division of ownership: what does **Terraform** own vs the **deploy pipeline** vs **manual operator apply**? | We have `ignore_changes=[task_definition]` (O-009). Is that the whole model, or a stopgap? |
| Q4 | Should `terraform-apply.yml` auto-apply staging at all — or is plan-only + manual apply safer? | **D-059 §9**: no auto-deploy while staging carries operational material. Auto-apply may violate that. |
| Q5 | How do module repos authenticate for cross-repo `repository_dispatch`? | **Open O-009 item** (deferred to arclight-complex). Recommendation on file: org-wide GitHub App. Currently needs a token with dispatch access to arclight-overcast. |
| Q6 | Prod deployment model? | Currently operator-only (D-059 §6). No pipeline. When/how does prod get a manual-approval gate? |

Deliverable of the scoping pass: a written model (candidate O-series decision) the reviewers can
check, then a plan to implement. Not a code change on day one.

---

## 5. NEXT SESSION — SECONDARY: Cloudflare → Terraform transfer

Bring Cloudflare under Terraform (codify DNS, the Access app, TLS). **D-058** gives Overcast domain
management. This is the highest-value infra-unification item after the deploy model.

**Coordinate with a parallel session** — there is stashed Cloudflare TF work that belongs to it:
```
stash@{0}: On main: cloudflare-adoption WIP — unblock parallel coturn plan
```
**Do NOT `git stash pop` blind** — it's another session's WIP. Inspect with `git stash show -p stash@{0}`
and coordinate before touching it.

Topology (from project memory): **2 Cloudflare accounts, 5 zones**. `arclightlabs.io` = canonical
web+email domain. Consolidation in progress (registrations moved, NS-flip pending). Tokens + DNS
backups in `~/.arclight` (§6).

---

## 6. Key references

### Credentials — `C:\Users\jkcos\.arclight\` (filenames verified; contents NOT read)

| File | Purpose |
|------|---------|
| `cloudflare_api_token` | Cloudflare **Account 1** API token |
| `cloudflare_api_token_2` | Second Cloudflare token (topology = 2 accounts — confirm which account before use) |
| `cf_access_service_token.env` | Staging Access bypass (health checks). **Expires 2027-07-09.** |
| `dns-backup/` | Per-zone JSON backups: `arclight-complex.net`, `arclightcomplex.com`, `arclightintel.com`, `arclightlabs.io`, `nerfherder.io` (5 zones) |

- Cloudflare **Account 1**: `76088d2d8df3d24e8808c3d26aeea51e`
- Staging **Access app**: `a378347b-e740-4d07-877b-892268ed395b`
- CF Access secrets in GitHub: `CF_ACCESS_CLIENT_ID` / `CF_ACCESS_CLIENT_SECRET`

### Tooling & account

- Terraform: `C:\Tools\terraform.exe` (**local v1.15.6**) — CI pins `~> 1.5` (mismatch, see §3.2a)
- AWS CLI: `"C:\Program Files\Amazon\AWSCLIV2\aws.exe"` (not on Bash PATH — use full path or PowerShell)
- AWS account **650880817826**, region **us-east-1**
- Repo: `https://github.com/arclightintel-dev/arclight-overcast.git`, state bucket `arclight-terraform-state`

### Deploy pipeline — cross-repo dispatch (O-009)

```bash
echo '{"event_type":"deploy-service","client_payload":{"module":"<m>","environment":"staging","image_tag":"<tag>"}}' \
  | gh api repos/arclightintel-dev/arclight-overcast/dispatches --input -
```
`<m>` ∈ {core, shuttleforge, podbay}. Workflow: verify ECR image → register task def → update service
→ wait stable → health-check `https://<m>.staging.arclight-complex.net/ready` through CF Access.
`workflow_dispatch` (manual UI trigger) is also wired.

### Manual apply pattern (until CI is fixed — see §3.2)

```bash
cd terraform/envs/staging
C:\Tools\terraform.exe apply -var="podbay_image_tag=<tag>"
```
Works locally because the gitignored `terraform.tfvars` supplies the four no-default vars
(`domain_name`, `alarm_email`, `github_org`, `core_image_tag`). Uses `john-admin` admin creds.

---

## 7. Governance / process notes

- **Spec→plan→review→implement paid off** for coturn (INV-005) — it had burned 4 apply-fix cycles
  before (O-002). Do not skip the spec step, even for "simple" modules.
- **D-065**: code lives in the plan, not the spec.
- **External reviewers** (codex-1 / ivan / mastermind / spinoza) are dispatched **by the user** —
  claude-main provides prompts only.
- **Branch-before-modify**: never modify files on `main`. (We are currently on `main` post-merge —
  branch before the next change.)
- **No-bundle**: no opportunistic fixes riding along with scoped work. Use
  `preserved / fixed / deferred` vocabulary in completion reports.
- **Every apply this session was manual** (local admin creds). CI auto-apply has never worked
  post-hardening — that reality is the whole reason for the §4 scoping brief. Do not paper over it.

### Housekeeping for next session

- Refresh `docs/PROJECT_STATE.md` (stale at `d1c9b7f`; update image tags to `v2-phase2-fix3` /
  `v1-fix2`, HEAD to `5f74a55`).
- Untracked `terraform/envs/staging/tfplan` in the working tree — a stale plan artifact, safe to
  delete (do not commit plan files).
